# Aura 灵犀招聘系统 - 开发进度总结 (数据库接入前阶段) - 备份文档

**日期**: 2026年4月10日
**项目状态**: MVP (最小可行性产品) 前端全量完成，AI 核心解析后端完成。当前处于本地模拟闭环阶段，准备接入关系型数据库。

---

## 1. 核心架构与技术栈
在接入真实的 MySQL 数据库之前，Aura 已经完成了一套完整的前后端分离架构验证：

### 前端 (Frontend)
*   **技术选型**: 原生 HTML5 + CSS3 (Vanilla JS)，无重量级框架依赖，极致轻量。
*   **UI/UX 风格**: 严格遵循 PRD 要求的 Apple 风格（极简留白、柔和阴影、深空强调色、微圆角）。
*   **状态管理**: 利用浏览器 `localStorage` 模拟了业务闭环，实现了数据的跨页面流转。
*   **组件化设计**: 虽然是原生 JS，但通过 `app.js` 统一渲染了全局侧边栏 (Sidebar) 和顶部状态栏 (Header)。

### 后端 (Backend)
*   **技术选型**: Python 3 + FastAPI。
*   **运行环境**: Uvicorn 提供极速的异步接口响应。
*   **AI 引擎**: 集成了 Google Gemini (gemini-1.5-flash) 与 `pdfplumber`。
*   **接口状态**: `main.py` 实现了基础健康检查与高复杂度的简历解析接口。

---

## 2. 已完成的核心模块

### 2.1 账号鉴权模块 (Auth)
*   **✅ 登录页 (`login.html`)**: 支持工作邮箱登录交互。
*   **✅ 注册页 (`register.html`)**: 支持企业空间注册交互。
*   **功能说明**: 实现了基础的路由守卫（未登录自动重定向）。

### 2.2 工作台看板 (`index.html`)
*   **✅ 数据可视化**: 完成了今日新增简历、待处理面试等 4 个核心数据指标卡的 UI 开发。
*   **✅ 任务列表**: 实现了 Trello 风格的「即将进行的面试」和带有微型数据漏斗的「活跃职位进度」组件。

### 2.3 候选人管理模块 (Core)
*   **✅ 候选人列表 (`candidates.html`)**: 
    *   完成了包含状态标签、经验学历的数据表格。
    *   **亮点**: 实现了高交互的「智能解析简历」拖拽上传模态框 (Modal)，包含动态上传进度条。
*   **✅ 候选人详情页 (`candidate-detail.html`)**:
    *   **双栏布局**: 左侧展示候选人基本概况和操作按钮。
    *   **多页签视图**: 
        1. 成功渲染了 AI 提取的核心技能标签 (Skill Tags) 和工作经历总结。
        2. 实现了基于时间的招聘动态流转日志。
        3. **亮点**: 通过 Base64 编码在浏览器内直接内嵌预览了用户上传的原始 PDF 文件。

### 2.4 职位与面试管理
*   **✅ 职位大厅 (`jobs.html`)**: 实现了响应式网格布局的职位卡片，直观展示了各职位的招聘数据漏斗，并做好了“热招”与“停招”的视觉区分。
*   **✅ 面试日程 (`interviews.html`)**: 实现了看板 (Kanban) 列式视图，按时间线排布了不同形式（线上/现场）的面试卡片。

---

## 3. 当前的数据流转机制 (V1.0)
为了在无数据库环境下进行产品演示，系统目前采用了以下临时数据流转方案：
1.  **文件上传**: 用户在前端上传 PDF，前端将文件转为 Base64 存入 `localStorage`，同时发送真实文件给后端。
2.  **后端处理**: FastAPI 接收文件，保存在操作系统临时目录 (`/tmp`)，调用 `pdfplumber` 提取文本后发送给 Gemini 大模型。
3.  **结构化返回**: 模型返回 JSON，后端将其传递给前端，并**立即销毁物理临时文件**。
4.  **前端存储**: 前端拿到 JSON 后，将其追加到 `localStorage` 的候选人数组中，详情页读取本地缓存进行渲染展示。

---

## 4. 下一步计划：真实数据库接入 (V2.0 正在进行中)
目前的本地模拟流转已经圆满完成了概念验证 (PoC)。接下来的开发重点是**数据持久化改造**：

*   **引入 ORM**: 使用 `SQLAlchemy` 对接 MySQL 数据库。
*   **设计数据表**: 建立完整的 `Candidate` 模型，支持复杂字段 (JSON, DateTime)。
*   **替换 LocalStorage**: 废弃前端基于缓存的逻辑，全面升级为通过 `fetch` 调用后端的真实 RESTful API（获取列表、获取详情）。
*   **文件落地**: 上传的 PDF 不再转换为 Base64，而是真实保存在服务器的 `uploads/` 目录中，数据库仅记录访问路径。